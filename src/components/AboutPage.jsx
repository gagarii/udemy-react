import React from 'react'; // React import

// Appクラスを定義
const AboutPage = () => (
  <div className="about">
    <h1 className="app-title">About</h1>
  </div>
);


export default AboutPage; // これで外部ファイルで使えるようにする
